import React, { useContext } from 'react';
import { rem } from 'polished';
import styled from 'styled-components';

const DeleteAllElement: React.FC = () => {
    return <div></div>;
};

export default DeleteAllElement;
